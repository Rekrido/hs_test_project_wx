
var substring = function (value,begainIndex,endIndex) {
	if (value) {
		value += ""
		return value.substring(begainIndex, endIndex)
	}else {
		return ''
	}
}

module.exports = {
  substring: substring
}